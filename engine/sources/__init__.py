"""Live data sources for the engine.

``nflverse`` builds the engine's :class:`~engine.data_loader.Slate` objects from
real nflverse data instead of the bundled sample file. See ``nflverse.py`` for
which feeds are used and which require GitHub release access.
"""
